export interface Product {
  category_id: number;
  category_slug: string;
  description: string;
  slug: string;
  id: string;
  image: string;
  name: string;
  price: number;
  composition: string;
  weight: string;
  quantity: number;
}
